target/

# Private plans and memory.
.claude

# Vendored third-party checkouts, each with its own .git.
ref/

# Device dumps, analysis and probe tools.
artifacts/

# Ad-hoc scripting: not part of the tool.
scripts/

# The assembled install tree, and what build.sh fetches to make it: the armhf
# glibc it links against and the Bluetooth stack it bundles, both pinned by URL
# and checksum. All build products — nothing here is put in place by hand, and
# no source file names any of it as a prerequisite.
deploy/

# macOS directory metadata.
.DS_Store
